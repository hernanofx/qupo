# Terraform (opcional)

Este directorio puede contener módulos y ejemplos para desplegar la infraestructura (EKS, RDS, S3, IAM, etc.).

Ejemplo: crear VPC, EKS cluster y RDS PostgreSQL. Crear módulos por cada recurso y usar variables para ambientes.
