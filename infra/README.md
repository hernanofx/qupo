# Infra

Carpeta para infraestructura como código (Terraform/Kubernetes manifests) y scripts de despliegue.

Inicialmente incluye:
- `k8s/` (manifests de ejemplo)
- `terraform/` (opcional)
- `docker-compose.yml` para desarrollo local
